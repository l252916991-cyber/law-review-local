# 前端功能验收记录（2026-09-08）

结论：未达到全部功能可用。已修复本轮确认的代码缺陷，模型和队列成功路径已补验；生命周期缺失入口和下列未覆盖项仍不能视为通过。

## 环境与证据

- 新启动 8891 服务，数据目录 `/tmp/lexvault-browser-audit`，只操作虚构材料；未修改用户案件。
- Microsoft Edge / Playwright CLI 实际点击，不模拟接口成功。
- 模型 URL 和 Redis 指向不可用的本地端口，检验离线行为，不代表部署了模型/队列后的结果。
- 完整离线命令按 `.github/workflows/test.yml` 的测试范围、socket 隔离和分支覆盖参数执行；日志位于 `output/frontend-audit/tests-final.log`，JUnit 和覆盖率文件同目录。后续已补验依赖导出、环境检查和发布清单。

## 当前结果

|功能|实际结果|
|---|---|
|六个主导航页面|可打开；不等于页面内所有功能通过|
|普通账号案件列表|发现 SQL 条件缺少 AND，已修复；权限测试 28 passed / 21 subtests passed|
|原页预览关闭按钮|发现 CSP 拦截内联 onclick；六处关闭按钮改用外部脚本绑定，浏览器关闭成功|
|目录校准与搜索|修改摘要、保存、以新摘要检索通过|
|标注新增|创建并显示标注、关闭弹窗通过|
|证据新增|创建、关闭弹窗、显示新增证据通过|
|TXT 上传|浏览器提交返回 201|
|批量上传|缺 Redis 时返回 503；补启隔离 Redis/worker 后浏览器提交 202，进度显示成功 1 个文件|
|本地证据分析|实际点击返回 200|
|疏漏检测|实际点击返回 200；未人工判定检测内容准确率|
|图谱适应画布、重新布局|点击完成|
|关闭模型后的普通聊天|提交返回 200；未据此声称模型答案质量通过|
|审阅包导出|浏览器收到 ZIP 下载；本轮未逐文件复核 ZIP 内容|
|Hybrid 索引、RAG 评测、双版本对比|真实本地模型接口成功：14 页索引、4 条 RAG 样例、双运行时对比；缺模型时改为 503 和明确提示，不静默降级|
|新建案件、归档取消与确认|浏览器操作检查，使用隔离数据|
|归档/回收站恢复 UI|浏览器跑通归档→恢复、移入回收站→恢复；最后一个案件移走后显示空状态，恢复后显示总览|
|永久删除 UI、自动清理|尚未完成，不能验收为可用|

## 回归与限制

本轮最终完整离线结果为 415 passed / 25 warnings，分支覆盖率 84.81%，原门槛 70% 保持不变（Python 3.14.5；未执行 CI 的 Python 3.11 矩阵）。修复案件权限列表 SQL、CSP 下六处弹窗关闭，以及明确离线检索仍调用远程模型的问题。远程模型失败继续中止；旧测试调整为检查错误脱敏、不持久化无效回答、缓存不被污染。

Ruff 全目录、配置范围内 Mypy、JavaScript 语法、uv pip check、doctor 和发布清单检查通过。requirements 两份导出清单已补齐锁文件中的 Excel 解析依赖。

真实模型验证使用独立 8892 服务及虚构案件：Qwen3 Embedding 4B（2560 维）和 bge reranker；RAG 样例 Recall@5=0.75，MRR=0.875，仅表示 4 条样例检索结果。双运行时对比关闭最终 LLM 生成，不作为生成质量验证。批量导入使用 8893、隔离 Redis 6391 和 worker。未把这些测试服务当作用户 8765 服务已更新的证据。

仍需继续验收：PDF/DOCX/图片 OCR 和多文件拖拽、证据及标注编辑/删除全部分支、批量任务进度及恢复、真实模型问答和 LangGraph/原生任务运行与恢复、结案模板打包内容、登录退出实际浏览器流程、各功能移动端和键盘异常边界。现阶段不得声称已把前端所有功能测试完毕。

## 归档恢复补验（2026-09-09）

新增已归档、回收站入口及恢复操作；案卷操作收进更多菜单。归档列表与回收站均按账号案件权限过滤，并测试越权恢复被拒绝。浏览器旧缓存曾导致新 HTML 搭配旧 JS，已增加资源版本和 no-cache 重新校验。验证使用重启后的隔离 8894 服务。

最终归档恢复回归：417 passed、25 warnings，覆盖率 85.40%；Ruff、Mypy、JavaScript 语法、发布清单通过。空工作区新建案件、菜单裁切列入浏览器复查。
