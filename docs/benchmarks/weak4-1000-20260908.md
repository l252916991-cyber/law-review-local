# 四类弱项改进与 1000 题验证

状态：已确认方案已替换候选路由，尚未完成改进后的 1000 题验收。

## 2026-09-09 实测进展

用户已确认 1-1 和 2-1 使用 `dev80-rag` 的方案。后续只对 3-2 和 3-8 继续选择，最终仍执行 20 类各 50 题。

| 开发方案（每类20题） | 1-1 | 2-1 | 3-2 | 3-8 |
|---|---:|---:|---:|---:|
| 旧10k回答同题复算 | 43.60 | 16.17 | 31.20 | 19.48 |
| dev80-rag | 72.48 | 42.74 | 33.31 | 18.41 |
| dev40-weak-v1 | — | — | 30.93 | 16.06 |

两个新实验均已完成离线复算，技术失败与截断均为零。`dev40-weak-v1` 的简洁回答及适用性提示未显示提分，保留为负结果。下一对照 `dev40-qwen36-guided` 使用已有本地35B模型、相同40道开发题，尚待结果；不凭模型规模选择。

35B 对照随后40次均返回 HTTP 507，属于服务容量失败，未取得能力对照证据。运行器已增加首次507后保留记录并停止的保护，专门回归验证仅调用一次。9B小请求实测正常，继续用固定两次调用的 `verify` 策略测试相同40题；复核仅读取题面与初稿，不提供参考答案。

工程状态：候选集成相关9项回归、ruff和mypy通过。上一轮完整离线检查为351通过、68失败，失败涉及已有模型降级行为与测试契约；依赖导出也发现差异。当前有并行检索和前端修改，最终需重新核对检查结果并分离提交范围。

目标是改进 1-1 法条背诵、2-1 法律文件校对、3-2 场景法律依据和 3-8 法律咨询，随后执行 20 类各 50 题的 1000 题真实本地模型复测。

## 冻结与比较

- 实验目录：`output/weak4-20260908`；由当前 `benchmark_campaign.py prepare` 创建，seed=8508。
- 1000 题锚点沿用 `output/benchmark_fixed_1000_20260907_8bit` 的题目清单。开发和确认清单在该锚点之外抽取，互不重叠。
- 整个 LawBench 已运行过，因此这里的确认集是本轮策略选择隔离集，不宣称从未接触过的盲测。
- 新旧同题比较使用 `output/benchmark_full_10k/run_20260907_8bit_task_guided/detailed_results.jsonl`，均以本轮冻结评分器复算。历史分数不直接代入差值。
- 旧 score85 campaign 因当前评分源码哈希不匹配而拒绝继续使用。本轮新目录冻结当前源码；不修改旧 manifest 或评分规则。
- campaign 工具自带的 `goal_score=85` 是历史模板字段。本次用户要求是四类改进后完成 1000 题验证；是否提分由同题实测决定。

## 实验顺序

1. 四类各 20 道开发题测试已有官方法条检索与校对格式修复，保留原始模型请求、回答、检索来源和后处理记录。
2. 基于开发结果实现必要的有版本号的候选策略，包括咨询长度控制、场景法条适用性核对；候选策略只使用题面与独立语料。
3. 四类各 10 道确认题验证入选策略；公开退化与失败。其他 16 类保持对照配置。
4. 冻结最终配置后执行全部 1000 题新推理，再做完整性、请求、来源哈希和评分复算验证，报告整体及四类同题差值。

开发集先使用 `benchmarks/score85/qwythos8bit-statutory-npc-v6.json`，Qwythos 9B 8-bit、本机端点、temperature=0、thinking=false。对后续策略采用独立配置与运行名，运行中不更改源码或语料。

工程修改按 AGENTS.md 完成受影响测试、静态检查及所需完整离线检查后本地提交。模型成绩与工程检查分开报告。

## 2026-09-09 候选路由替换

`weak4-candidate-v1.json` 已加入 npc_v6/v7 和 300 条部门规章补充集。候选路由不再使用开发集上退化的
3-2/3-8 自定义提示：1-1、2-1、3-2 走已保留的 `statutory_rag`，3-8 与其余任务保持原 `task_guided`。
3-2 部门规章方案在 development 20 题和 confirmation 10 题均无逐题回退，详细分数见
`score-uplift-20260909.md`。这次替换不代表 1000 题验收已经完成。

后续组合评测使用此入口（每类 10 题，共 200 题确认集）：

```sh
uv run --locked python scripts/benchmark_experiment.py \
  --directory output/weak4-20260908 --name promoted-confirmation-200 \
  --profile-file benchmarks/score85/weak4-candidate-v1.json \
  --split confirmation --per-task 10
```

旧运行目录保存了当时的配置与源码快照；替换后的配置仅用于新运行，不能用于续跑旧实验。
