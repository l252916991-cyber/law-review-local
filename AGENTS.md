# 项目开发与 Git 提交规则

本文件适用于本仓库的所有开发与维护任务。执行任务前先阅读本文件；子目录有补充规则时，一并遵守。用户在当前任务中的明确要求优先。

## 1. 默认工作流程

1. 检查当前分支、`git status --short --branch` 和已有差异，明确本次任务范围。
2. 阅读相关实现、测试和配置，以当前代码与运行行为为依据。
3. 完成一个主题明确、可以独立验证的改进，并同步必要的测试与文档。
4. 运行与改动风险相匹配的检查，审查最终 diff 与待提交文件。
5. 检查通过后创建本地原子提交，并报告提交哈希、改动内容、验证结果和遗留问题。

后续用户要求实施项目改进时，默认包含完成验证后创建本地 Git 提交的授权；用户明确要求暂不提交时遵从其要求。仅咨询、诊断、审查或报告状态时，不自动修改或提交代码。

## 2. 提交粒度与分支

- 每完成一个独立、完整、可验证的改进就提交；无需每次保存文件都提交。
- 一个提交只处理一个主题。功能、修复、相关测试与必要文档可以一起提交；无关清理和格式化单独处理。
- 大任务拆成可独立验证的提交，保证每个提交的代码与说明一致。
- 有基线提交后，新任务默认使用 `codex/<简短主题>` 分支；已处于合适的任务分支时继续使用。
- 仓库尚无提交时，首次基线需要单独审查文件范围与验证状态。除非用户明确要求初始化基线，不将全部现有未跟踪文件作为本次改动提交；报告这一前置条件。
- 本地提交不等于推送或发布。推送、合并与发布按用户任务授权执行。

## 3. 提交前检查

- 只暂存本次任务涉及的明确路径，优先使用 `git add -- <路径>`；避免无差别暂存整个工作区。
- 审查 `git diff`、`git diff --cached --stat` 和 `git diff --cached`，再运行 `git diff --cached --check`。
- 保留用户已有改动；遇到与本任务重叠且无法可靠分离的改动时，先说明冲突并请求方向。
- 不提交密钥、真实案件材料、个人数据、运行数据库、模型权重、日志、缓存和临时产物。`.env.example` 只能包含安全示例值。
- 新增文件检查是否被 `.gitignore` 意外忽略；调整忽略规则时保持现有隐私保护范围。
- 不通过跳过钩子、降低覆盖率门槛、删除失败测试或弱化断言来获得通过结果。
- 不自动使用破坏性恢复、强制推送或改写已有历史；需要这些操作时必须有明确授权。

## 4. 验证要求

以 `pyproject.toml`、`.pre-commit-config.yaml` 和 `.github/workflows/test.yml` 中的当前配置为准。配置更新时同步本文件中的示例。

- 纯文档或规则调整：核对内容、路径、命令与 Git 差异；无需运行无关的业务测试。
- Python 代码调整：运行受影响测试及 `uv run --locked ruff check app tests scripts`；涉及工程脚本或类型配置时运行 `uv run --locked mypy`。
- 前端脚本调整：运行 `node --check app/static/app.js`，并验证受影响的实际页面流程。
- 修改业务行为或修复缺陷时，按风险补充有实际断言价值的测试，覆盖正常流程与相关异常、边界条件。
- 跨模块、数据结构、检索链路、依赖或 CI 调整，以及准备合并时，执行现有 CI 对应的完整检查，包括离线测试、覆盖率、依赖导出一致性与发布清单检查。
- 集成测试使用隔离数据目录和数据库，禁止写入真实案件数据。默认离线执行；需要外部服务或付费模型时先确认任务授权和环境。
- 声称 HTTP/UI 集成验证通过前，确认运行进程加载了本次代码，并实际执行相关流程。
- 测试后若继续修改代码，重跑受影响的检查；验证结果必须对应最终提交内容。

常用静态检查命令：

```sh
uv run --locked ruff check app tests scripts
uv run --locked mypy
node --check app/static/app.js
```

完整离线测试的环境隔离、插件和覆盖率参数直接参考 `.github/workflows/test.yml`，不要将简化的局部测试描述为完整 CI 通过。

检查失败时先定位原因：本次引入的问题应修复后再提交；已有失败或缺失依赖应明确记录，不能声称全部通过。必要检查无法完成时，保留改动并说明阻塞；仅在用户明确要求保存未完成状态时创建标明状态的草稿提交。

## 5. 提交信息

使用 `<type>(<可选范围>): <具体改动>`，描述实际结果。类型使用 `feat`、`fix`、`test`、`docs`、`refactor`、`perf`、`build`、`ci` 或 `chore`。

示例：

```text
fix(retrieval): 修复证据更新后的索引同步
test(database): 补充测试数据库隔离验证
docs: 添加项目开发与提交规则
```

复杂改动在提交正文中说明原因、关键影响和验证结果。避免仅使用“更新”“优化”或“修复问题”等无法识别内容的说明。

## 6. 完成交付

交付时简要说明改了什么、为什么、如何验证，并提供本地提交哈希和工作区状态。未提交时说明具体原因。

验证结论必须区分局部测试、完整离线检查、模型基准和真实业务流程；未执行、跳过或失败的项目如实标明。生产就绪或业务效果结论需要对应证据。
